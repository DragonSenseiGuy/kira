diff --git a/app/assets/base.css b/app/assets/base.css
index 40135d7..49915a2 100644
--- a/app/assets/base.css
+++ b/app/assets/base.css
@@ -1124,6 +1124,11 @@ textarea::placeholder {
   animation: uEnter var(--duration-enter) var(--ease-out-strong) both;
 }
 
+/*
+  Ends on `transform: none`, so with `both` fill-mode it permanently WIPES any
+  positioning transform on the element it lands on. Anything animated with
+  uEnter must position itself with insets/margins, never a translate.
+*/
 @keyframes uEnter {
   from {
     opacity: 0;
