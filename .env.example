NUXT_SESSION_SECRET=your-session-secret-here

# Password authentication (optional - gates the entire app behind a password)
# If not set, the app is open to anyone
NUXT_APP_PASSWORD=

# Shared API key (optional - provides an API key for all users)
# If not set, each user must provide their own key in settings
NUXT_OPENAI_API_KEY=
